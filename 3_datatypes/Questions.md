# Topic 3: Data Types

## Exercise 1:
Create variables using 5 different built-in Python data types (e.g., int, float, str, bool, list). Print each variable along with its data type using the type() function.

Example:

python
x = 10
print(x, type(x))

## Exercise 2:
Create a variable holding a number (e.g., age = 20) and check its type using type(). Then reassign the same variable to hold a string value (e.g., age = "twenty") and check its type again. Add a comment explaining why Python allows this — i.e., why variables in Python are "dynamically typed" (a variable's type can change based on the value it holds).

## Exercise 3:
Use type() to check and print the data type of each of the following:

True
3.14
"Hello"
[1, 2, 3] (a list)
(1, 2, 3) (a tuple)

Bonus: Look up the basic difference between a list and a tuple, and write it in your own words as a comment.