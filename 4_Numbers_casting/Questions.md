# Topic 4: Numbers & Casting

## Exercise 1:
Create 3 variables — one int, one float, and one complex number (e.g., 5+3j). Print each along with its type using type().

## Exercise 2:
Practice type casting with the following conversions:

Convert a string number to an int (e.g., "25" → 25) using int()
Convert an int to a float (e.g., 10 → 10.0) using float()
Convert a float to an int (e.g., 9.8 → 9) using int() — print the result and observe whether it rounds or truncates
Convert an int to a string (e.g., 25 → "25") using str()

Print the type after each conversion to confirm it worked.

## Exercise 3:
Try running this line:

python
x = "Iqra" + 5

Note the error you get. Then fix it two different ways:

By converting the number to a string using str()
By changing the expression so there's no type mismatch

Add a comment explaining why the original error occurred.